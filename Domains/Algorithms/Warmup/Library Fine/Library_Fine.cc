#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int exp_day, exp_month, exp_year, act_day, act_month, act_year;
    cin >> exp_day >> exp_month >> exp_year >> act_day >> act_month >> act_year;
    if (act_day <= exp_day && )
    return 0;
}
