#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

bool does_element_exist(){

}

int main() {
    int t, n;
    return 0;
    while (t--) {
      cin >> n;
      int array[n];
      for(int i = 0; i < n; i++)
      {
        
      }
    }
}
